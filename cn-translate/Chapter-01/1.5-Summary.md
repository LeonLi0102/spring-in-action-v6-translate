## 1.5 小结

* Spring 的目标是让开发者轻松应对各种挑战，比如创建 Web 应用、使用数据库、保护应用安全以及构建微服务。
* Spring Boot 构建在 Spring 之上，简化了依赖管理、自动配置和运行时监控，让 Spring 变得更加简单。
* Spring 应用可以使用 Spring Initializr 进行初始化，它是一个基于 Web 的工具，大多数 Java 开发环境都支持。
* 在 Spring 应用上下文中，组件（通常称为 bean）可以通过 Java 或 XML 显式声明，也可以通过组件扫描自动发现，或者由 Spring Boot 自动配置。
